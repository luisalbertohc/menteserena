import AuthLayout from '@components/layout/AuthLayout';
import NewPassword from '@components/auth/forms/NewPassword';

const NewPasswordScreen = () => {
  return (
    <AuthLayout>
      <NewPassword />
    </AuthLayout>
  );
};

export default NewPasswordScreen;
